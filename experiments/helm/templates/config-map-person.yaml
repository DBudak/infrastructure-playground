apiVersion: v1
kind: ConfigMap
metadata:
  name: config-map-person
data:
  name: {{ .Values.personName }}
  lastname: {{ .Values.personLastName }}
  cat.properties: |
    species.name=feline,animal
    designation=sleep,eat